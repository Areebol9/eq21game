"use strict";

// Deployment builds overwrite this file with the public Cloudflare Worker URL.
// Leaving it empty keeps local development on the normal localhost/same-origin fallback.
window.EQ21_ONLINE_URL = window.EQ21_ONLINE_URL || "";
